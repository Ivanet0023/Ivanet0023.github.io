{
  "name": "lab5-foodex-deployment",
  "version": "1.0.0",
  "description": "Root package.json for Render deployment",
  "scripts": {
    "build": "cd foodex-app && npm install && npm run build && cd ../server && npm install && npx prisma generate && npx prisma migrate deploy",
    "start": "node server/server.js"
  }
}
